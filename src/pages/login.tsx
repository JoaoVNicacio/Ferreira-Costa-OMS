import * as W from '../styles/wrapper'
import LoginComponent from '../components/Login'

export const Login = () => {
    return (
        <W.WrapperLogin>
            <LoginComponent/>
        </W.WrapperLogin>
    )
}